{
    "configurations": [
      {
        "type": "node-terminal",
        "name": "dev",
        "request": "launch",
        "command": "yarn dev ${file}",
        "cwd": "${workspaceFolder}",
        "env": {
        },
        "resolveSourceMapLocations": [
          "!**/node_modules/**",
          "!node:internal/**"
        ]
      }
    ]
}
